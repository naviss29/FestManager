spring:
  datasource:
    # Local Docker : défauts postgres/festmanager
    # Railway : surcharger avec SPRING_DATASOURCE_URL / USERNAME / PASSWORD
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/festmanager}
    username: ${SPRING_DATASOURCE_USERNAME:festmanager}
    password: ${SPRING_DATASOURCE_PASSWORD:festmanager_dev}
    hikari:
      maximum-pool-size: 3    # réduit le pool de 10 (défaut) à 3 pour limiter la mémoire
      minimum-idle: 1

  jpa:
    open-in-view: false           # désactive l'OSIV (économise mémoire + supprime le WARN au démarrage)
    hibernate:
      ddl-auto: update            # adapte le schéma si changement de colonne (ex: categorie enum→String)

  # SMTP — variables d'environnement (Docker ou Railway)
  mail:
    host: ${SMTP_HOST:localhost}
    port: ${SMTP_PORT:1025}
    username: ${SMTP_USER:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail.smtp.auth: ${SMTP_AUTH:false}
      mail.smtp.starttls.enable: ${SMTP_TLS:false}

# Railway injecte PORT automatiquement — fallback 8080 pour Docker local
server:
  port: ${PORT:8080}

springdoc:
  api-docs:
    enabled: false  # Swagger désactivé en prod — réduit la mémoire (scanning 10+ controllers évité)
  swagger-ui:
    enabled: false

# Actuator : expose uniquement /actuator/health pour le health check Railway
# Les autres endpoints (metrics, env, beans…) restent désactivés pour la sécurité
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

app:
  uploads:
    # Répertoire de stockage des fichiers — monter un volume Docker persistant sur ce chemin en production
    dir: ${UPLOADS_DIR:./uploads}
  # CORS : liste d'origines séparées par des virgules
  # Railway : mettre l'URL du service frontend (ex: https://festmanager.up.railway.app)
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:4200,http://localhost:80,http://localhost}
